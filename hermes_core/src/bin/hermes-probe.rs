//! hermes-probe — desktop integration gate binary (T2 fills it in).
//! Stub for stream 1: the full probe (login, connect, prompt, JSONL record)
//! arrives with the WebSocket client.

fn main() {
    println!("hermes-probe: stub — implemented in T2");
}