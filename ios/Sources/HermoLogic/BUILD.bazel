load("//platforms:apple.bzl", "APPLE_ONLY")
load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

swift_library(
    name = "HermoLogic",
    srcs = glob(["*.swift"]),
    target_compatible_with = APPLE_ONLY,
    module_name = "HermoLogic",
    visibility = ["//visibility:public"],
)
