package(default_visibility = ["//visibility:public"])

# Android platform() targets for the --platforms / --android_platforms flags.
# Apple builds select on @build_bazel_apple_support//configs instead.
