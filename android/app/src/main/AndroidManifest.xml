<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sh.mo">

    <!-- The app talks to the self-hosted Hermes gateway over the tailnet. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- T11: the ConnectivityManager network callback (offline → retry when
         the network returns) requires the network-state query permission;
         normal, not runtime-requested. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- QR pairing without Google Play Services: our own CameraX+ZXing scan
         screen (T6b). Requested at runtime before the camera opens; a
         denial shows a readable line, never a crash. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".HermesApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hermo">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Hermo"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Shared pairing links (hermes://...?url=...&user=...). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="hermes" />
            </intent-filter>
        </activity>
    </application>

</manifest>
